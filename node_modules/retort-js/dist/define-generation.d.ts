import { RetortSettings, RetortRole } from "./agent";
import { RetortConversation } from "./conversation";
import { RetortMessage } from "./message";
export declare function defineGeneration(conversation: RetortConversation, role: RetortRole, push: boolean): (generationSettings?: Partial<RetortSettings>) => Promise<RetortMessage>;
